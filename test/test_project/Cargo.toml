[package]
name = "test_project" # name side comment
version = "0.1.2" # A version side comment
edition = "2024"
# Super description here:
description = "A test app that will stay open until killed"

[dependencies]
serde = {version = "1.0.219"}